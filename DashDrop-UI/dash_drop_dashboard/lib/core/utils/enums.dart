enum RequestState {
    loading,
    loaded,
    error
}
enum NameState {
  noThing,
  create,
  get,
  getDetails,
  update,
  delete
}

enum Gender {
  male,
  female
}
enum SensitivityLevels {
  low,
  medim,
  high,
  critical,
}
enum VehicleTypes {
  bicyle,
  motorcycle,
  car,
  van,
  pickTruck,
  truck
}